package varargsimport varargs.sumimport atomicTest.eqfun main() {    // 스프레드 연산자(*)를 사용하면 Array 타입의 객체를 vararg의 파라미터 인자 목록으로 변환이 가능하다.    // arrayOf 대신에 intArrayOf를 사용한 이유는 vararg은 Array<Int>타입이 아닌, IntArray 타입을 인자로 받아야 하기 때문이다.    // 이 두 타입은 JVM에서 바이트코드로 변환될 때 차이가 있다.    // 1. Array<Int>는 Java의 Wrapper 타입 Integer[]로 변환되고,    // 2. IntArray는 Java의 Primitive 타입 int[]로 변환된다.    val array = intArrayOf(4, 5)    sum(1, 2, 3, *array, 6) eq 21    // sum(1,2,3, array, 6)은 컴파일 되지 않는다.    val list = listOf(9, 10, 11)    sum(*list.toIntArray()) eq 30}